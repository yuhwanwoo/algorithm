package study;

import java.util.HashMap;

public class commerce01 {

	public static void main(String[] args) {
		int[] gift_cards= {4,5,3,2,1};
		int[] wants= {2,4,4,5,1};
		
		solution(gift_cards,wants);
		
	}
	public static int solution(int[] gift_cards, int[] wants) {
		HashMap<Integer, Integer> hash=new HashMap<>();
		
		for (int i = 0; i < gift_cards.length; i++) {
			hash.getOrDefault(gift_cards[i], 0);
			hash.put(gift_cards[i], hash.getOrDefault(gift_cards[i], 0)+1);
		}
		
		return 0;
	}

}
